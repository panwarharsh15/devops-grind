apiVersion: app/v1
kind: Deployment
metadata:
  name: go-server-app
spec:
  selector:
    matchLabels:
      app: go-server-app
  template:
    metadata:
      labels:
        app: go-server-app
    spec:
      containers:
      - name: go-server-app
        image: ghcr.io/panwarharsh15/devops-grind:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 3000
